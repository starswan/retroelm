<%= form_for @game, :html => { :class => "form-horizontal game" } do |f| %>

    <% if @game.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @game.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%= error_span(@game[:name]) %>
  </div>
  <div class="control-group">
    <%= f.label :tapfile, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :tapfile, :class => 'form-control' %>
    </div>
    <%= error_span(@game[:tapfile]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            games_path, :class => 'btn btn-default' %>

<% end %>
